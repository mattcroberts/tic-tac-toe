mutation newGame {
    newGame {
        id
        gridItems {
            id
            player {
                id,
                symbol
            }
        }
        currentPlayer {
            id
            symbol
        }
        isFinished
    }
}
