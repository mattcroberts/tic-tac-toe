mutation executeTurn($id: ID!, $player: String!, $x: Int!, $y: Int!) {
    executeTurn(id: $id, player: $player, x: $x, y: $y) {
        id
        gridItems {
            id
            player
        }
        currentPlayer
        winner
        isFinished
    }
}
