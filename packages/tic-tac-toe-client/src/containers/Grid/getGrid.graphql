query GET_GRID($id: ID!) {
    grid(id: $id) {
        id
        gridItems {
            player
        }
        currentPlayer {
            id
            symbol
        }
        winner {
            id
            symbol
        }
        isFinished
        size
        gameUrls {
            NAUGHT
            CROSS
        }
    }
}
