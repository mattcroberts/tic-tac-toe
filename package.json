{
	"devDependencies": {
		"lerna": "^2.5.1",
		"npm-run-all": "^4.1.3"
	},
	"scripts": {
		"dev": "run-p -l start-ui-dev start-api-dev",
		"start-ui-dev": "lerna exec --scope tic-tac-toe-client -- PORT=3001 yarn start",
		"start-api-dev": "lerna exec --scope tic-tac-toe-server -- yarn dev",
		"build": "docker-compose build",
		"start": "docker-compose up"
	}
}
